@model ICollection<string>

<h4>Советуем ещё...</h4>
@if (Model.Count > 0)
{
<ul class="collection">
    @foreach (var item in Model)
    {
        <li class="collection-item">@item</li>
    }
</ul>
}
else
{
    <br>
    <h5>...а впрочем ничего.</h5>
}

